document.addEventListener('DOMContentLoaded', function() {
    var targetEl = document.querySelector('h3'); // Modify the selector to target the desired element
    targetEl.style.color = 'red'; // Add a temporary style change to the targeted element
});
